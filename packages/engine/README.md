# Engine

文章を表示単位へ分け、読む速さと現在位置を計算します。

## 入出力

文字列を`ReaderUnit[]`へ分割します。各単位には、原文、文番号、種類、開始位置、終了位置が入ります。位置の単位はUTF-16コード単位です。

表示時間、前の文、現在の見出し、読書進捗、原文位置と表示単位の対応もEngineが計算します。型と公開APIは`src/types.d.ts`にあります。

## 不変条件

- 原文の文字列と順序を保つ
- すべての表示単位に原文上の開始位置と終了位置を持たせる
- 同じ入力とロケールから同じ結果を返す
