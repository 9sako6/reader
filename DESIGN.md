# デザイン

Readerのデザインは、読む対象を主役にする。SpotsとPageは別のコンテンツではなく、同じ文書と読書位置を異なる形式で示す表示モードとして扱う。

## 判断の優先順位

設計上の条件が競合した場合は、次の順で判断する。

1. 原文の内容、順序、現在の読書位置を保つ
2. 本文を読みやすくし、操作できる状態を保つ
3. 読書の焦点と主要な操作の位置を安定させる
4. ミニマップ、前後の文、進捗などの補助情報を示す
5. 装飾を加える

下位の要素を守るために、上位の要素を移動、変形、欠落させない。

## 表示モード

モードの表示順は`Spots`、`Page`とする。モードを切り替えても、文書、現在の読書位置、進捗を保つ。切り替えによって読書領域や主要な操作の位置を動かさない。

### Spots

- 現在の語句を、読書領域の水平中央と垂直中央に置く
- 前後の語句、ミニマップ、進捗、操作UIによって、現在の語句の中心を動かさない
- 本文、引用、補足では、同じ中心を使う
- 引用や補足の背景は本文と同じ中心に置き、本文の位置や操作UIを動かさない
- 原文の文字列、順序、意味を保ったまま、表示に収まる意味のまとまりへ分割する
- 語句が長い場合は、文字を場当たり的に縮小せず、意味のまとまりを保って表示単位を組み直す

### Page

- 本文カラムを、読書領域の水平中央に置く
- 本文カラム内の文章は左揃えにする
- 見出し、本文、引用、コード、画像は、本文カラムの基準から不用意に外さない
- ミニマップなどの補助UIを追加しても、本文カラムの中心を動かさない
- スクロール位置から現在の読書位置を求め、Spotsへ切り替えたときに同じ位置を示す

## センタリング

中央に置く要素を実装する前に、対象と基準領域を決める。

- Spotsの語句：読書領域に対して中央に置く
- Pageの本文：本文カラムを読書領域に対して中央に置き、文章はカラム内で左に揃える
- アイコン：見えている線ではなく、操作領域の矩形に対して中央に置く
- 背景や装飾：対応する内容と同じ中心を使い、内容の配置計算には参加させない

アイコンの位置を文字のベースラインや行の高さに依存させない。補助UIの幅を相殺するために、本文へ反対向きの余白を加えない。

## 視覚的な優先順位

視覚的な強さは、現在の本文、選択中のモードと主要操作、選択可能な操作、進捗と見出し、前後の文と補助情報の順にする。

通常の表示では、SpotsとPageの選択状態やホバー状態を文字の明るさで示す。ホバー時に背景色を加えない。キーボードフォーカスや高コントラスト表示では、状態を判別するために輪郭や背景を使用できる。

## 余白と衝突

余白は、要素同士の関係を示すために明示して設ける。本文とミニマップを接触、重複させない。表示領域が足りない場合は、次の順で調整する。

1. 補助情報を隠す
2. 補助情報の内部をスクロールできるようにする
3. 補助情報の位置を変える
4. 本文の読みやすさを損なわない範囲で本文幅を調整する

装飾のために本文の幅や中心を変えない。

## 操作

- 操作領域と、その中に描くアイコンの大きさを分けて考える
- 主要な操作領域は、44×44 CSSピクセルまたは各プラットフォームで同等の大きさを下回らない
- タッチ操作だけですべての操作を完了できるようにする
- ホバーは操作の成立条件にせず、利用できる環境で補助的な反応として使う
- 選択状態、無効状態、フォーカス状態を支援技術から判別できるようにする
- 操作後も、読んでいる位置と主要な操作の配置を保つ

## 画面サイズとプラットフォーム

画面サイズやプラットフォームが変わっても、情報の意味、名称、順序、状態遷移を共通にする。配置と情報量は利用できる領域に合わせて調整する。

小さい画面では、本文と主要操作を優先し、補助情報を減らす。ポインターのない環境を基準に操作を成立させ、ポインターがある環境では同じ操作へフォーカスとホバーの反応を加える。

## 状態の変化

読み込み、再生、一時停止、エラー、モード切り替えの前後で、読書領域の骨格を不用意に変えない。待ち時間が知覚できる場合は進行中であることを示し、短い待ち時間では表示を点滅させない。

動きを減らす設定では、意味を失わない範囲でアニメーションを停止する。コントラストを上げる設定では、通常時の抑制よりも状態の判別を優先する。

## デザイン検証

デザイン変更では、見た目だけでなく、次の結果を確認する。

- Spotsの語句が読書領域の中央にある
- Pageの本文カラムが読書領域の中央にあり、文章が左揃えである
- アイコンが操作領域の中央にある
- モードを繰り返し切り替えても、現在の読書位置が変わらない
- モードを切り替えても、読書領域と主要な操作が動かない
- 本文とミニマップの間に余白があり、重ならない
- SpotsとPageのホバー時に背景色が加わらず、文字が明るくなる
- 長い見出し、長い語句、コード、画像でも、本文や操作が欠けない
- スマートフォン相当の画面で、ホバーなしですべての操作を完了できる
- キーボード、読み上げ、高コントラスト、文字サイズの拡大、動きを減らす設定で状態を判別できる

自動テストでは、色や座標などの実装値だけでなく、中央、継続性、非重複、操作可能性といった利用者が観測できる結果を検証する。
